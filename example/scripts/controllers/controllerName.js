export class controllerName {
                              constructor(Data) {
                                                  Data.load().then((data) => this.data = data);
                                                }
                            }
